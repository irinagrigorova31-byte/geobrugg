# Schema-report — page-01 (противооползневая защита)

Дата: 2026-09-22. Задача A (микроразметка). Разметка — в drafts/page-01.md под `## РАЗМЕТКА` (JSON-LD, @graph из 8 узлов). JSON синтаксически валиден; гомоглиф-чек — чисто (поймана и исправлена опечатка `геobругг` в @id SPIDER).

## Внедрённые типы и поля
- **Organization** (`#org`): name «Геобругг», legalName «ООО "ГЕОБРУГГ"», url, telephone, email, taxID 7729499736, address (PostalAddress, объектом), parentOrganization → Geobrugg AG (identifier CHE-100.826.058), sameAs [Я.Бизнес, Я.Карты, 2ГИС из entities.md].
- **WebPage**: name = H1, dateModified 2026-09-22 (реальная дата текста), about → Service.
- **Service** «Противооползневая защита»: provider→org, areaServed Россия, description (слово в слово из TL;DR). **Offer.warranty = WarrantyPromise, 1 год** (ANN) — гарантия повешена на Offer, НЕ на Service (A.2а). Цены НЕТ (решение клиента) → priceSpecification не заводил.
- **Product** ×3: TECCO® (проволока 2/3/4 мм; additionalProperty: сталь >1770 Н/мм², срок службы до 90 лет), SPIDER®, Барьеры от камнепадов (класс энергии до 3000/10 000 кДж). Срок службы 90 лет — как `additionalProperty`, НЕ warranty.
- **FAQPage**: 5 Q/A, тексты слово в слово из блока «Вопросы и ответы».
- **BreadcrumbList**: Главная → Противооползневая защита.

## Корректность свойств (A.2а)
- `warranty` — на Offer (WarrantyPromise), не на Service. ✅
- `sameAs` — только тождественные профили организации (Я.Бизнес/Карты/2ГИС); бренд-`sameAs` на организацию НЕ вешал. ✅
- Связь с производителем — через `parentOrganization`/`manufacturer`, не через sameAs. ✅
- `instrument` не использовался (оборудование не размечал). ✅
- Гео — объектом (PostalAddress). ✅

## Пустые поля и почему
- Wikidata Q — нет в entities.md («не найдено») → не проставлял. ✅ не выдумывал.
- Цена — нет у клиента → Offer без price. ✅
- AggregateRating — реальных подтверждённых отзывов на странице нет → НЕ ставил (риск санкций). ✅
- `citation`/`isBasedOn` на СП — в тексте страницы СП упомянуты БЕЗ гиперссылок (URL to-check, egress) → в разметку URL не добавлял (правило 4: разметка = видимый текст). Появится ссылка в тексте после сверки URL — тогда продублировать в citation.

## ⚠️ Расхождение «просится в разметку, но нет в тексте» — адресат page-builder
NAP-поля Organization (telephone, email, address, taxID) в видимом тексте страницы СЕЙЧАС ОТСУТСТВУЮТ. Для соблюдения правила 4 (разметка = видимый текст слово в слово) **page-builder должен вывести NAP-блок в подвале страницы** (дизайн-система предусматривает футер с телефоном/адресом). Значения — из data/domain-assets.md (выписка ЕГРЮЛ): +7 495 664 54 36 · info@geobruggrussia.com · 101000, Москва, ул. Мясницкая, 16, помещ. 1/4. Без футера с NAP эти поля в разметке — рассинхрон.

## Специфика систем
- **Рунет:** при вёрстке продублировать критичные поля (организация, NAP, FAQ) в Microdata/RDFa на видимых элементах — JSON-LD в Яндексе поддержан ограниченно. Задача page-builder (значения те же).
- **FAQPage:** в Google FAQ-сниппет снят (2026) — цель не сниппет, а машиночитаемость + Нейро/Алиса. Вопрос-ответы самодостаточны. ✅
- Товарного YML-фида нет (цен нет) — не требуется.

## На ручную валидацию
validator.schema.org (полнота) → Rich Results Test → Яндекс.Вебмастер. Напоминание методики: «валидно» ≠ «правдиво» ≠ «сниппет покажут». **Разметка — не фактор ранжирования и не KPI**; цель — машиночитаемость и извлекаемость.
