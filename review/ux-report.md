# UX-report — preview/page-01.html

Дата: 2026-09-22. PRODUCT.md — нет; проектный скилл project-geobrugg — есть (ЦА B2B/инженеры → плотность фактов уместна, кегль стандартный). Стиль — из input/brand.md.

## Проверено (2.1)
- **Контраст (по числам, WCAG):**
  - ink #141414 / surface #ffffff — ~19:1 ✓
  - ink-muted #5f6266 / #ffffff — ~5.9:1 ✓ (≥4.5)
  - on-navy #ffffff / surface-navy #2b3a8c — ~8.4:1 ✓ (CTA, шапка таблицы)
  - navy-ссылки #2b3a8c / surface-sub #dde0e6 (футер) — ~6.6:1 ✓
  - ink #141414 / surface-sub #dde0e6 (TL;DR, figcaption-фон) — ~14:1 ✓
  Провалов нет.
- **Иерархия:** один H1 → H2 → H3 (системы, FAQ), пропусков уровней нет.
- **Первый экран:** H1 + TL;DR + паспорт-абзац (число «до 90 лет», «до 10 000 кДж», атрибуция «по данным „Геобругг“») + первый H2 «Срок службы до 90 лет» — дифференциатор вверху, не ниже сгиба. ✓
- **Таблица:** 1 таблица (4 колонки) в обёртке `.table-wrap` c `overflow-x:auto`; `<thead>/<th>`, чёткие заголовки. ✓
- **Инфографика:** 2 инлайн-SVG, `max-width:100%`, подписи-figcaption; данные схем совпадают с текстом (кДж, ×5 — есть в тексте рядом). ✓
- **Alt/доступность:** SVG с `role="img"` + `aria-labelledby` (title/desc); figcaption — содержательные подписи, не «инфографика». ✓
- **Мобильная (320/375/768):** viewport-мета; основной текст 16px; `max-width:100%` на svg; таблица прокручивается; CTA-ссылка ~44px по высоте (padding 12×24); горизонтального скролла нет (кроме обёртки таблицы). H1 уменьшается до 28px на ≤400px. ✓

## impeccable detect
`npx impeccable detect` не запущен: egress-политика окружения блокирует загрузку npm-пакета в рантайме. Ручной чек по чек-листу 2.1 пройден (см. выше). При открытии сети — прогнать `npx impeccable detect preview/ --json` и разобрать находки.

## Исправлено на стадии 2
Правок по рендеру не потребовалось — вёрстка стадии 1 собрана сразу по дизайн-системе (токены brand.md, прямые углы, Manrope/PT Sans, синий #2b3a8c). 

## Требует решения (адресаты)
- **og:image PNG** ещё не сгенерирован (путь в разметке проставлен): `assets/hero/protivoopolznevaya-zashchita-og.png` — задача человеку по брифу hero-maker. Не блокер вёрстки.
- **Заглушка `[ТРЕБУЕТСЯ: кейсы клиента]`** в блоке «Реализованные проекты» — перенесена как есть. Страница НЕ упаковывается в delivery, пока клиент не вставит кейсы (или не подтвердит публикацию без блока). Превью — допустимо.
- **NAP-футер** добавлен (тел./адрес/e-mail/ИНН/ОГРН) — теперь Organization в JSON-LD совпадает с видимым текстом (снят флаг schema-report).

## Статус первого экрана: дифференциатор виден ✓
