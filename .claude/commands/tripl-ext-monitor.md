---
description: "⛔ ЗАГЛУШЕНА (отключена). Off-site GEO-мониторинг (geo-monitor) — замер закрытия citation-gap. Агент отключён; команду не запускать до включения."
---

> ⛔ **КОМАНДА ЗАГЛУШЕНА.** Агент `geo-monitor` отключён (см. баннер в `.claude/agents/geo-monitor.md`).
> Не запускать. Замер закрытия citation-gap сейчас делается вручную. Включить — снять пометки ЗАГЛУШЕН
> в агенте, `CLAUDE.md`, `ORCHESTRATION.md` и этой команде. Ниже — исходная логика (сохранена).

Выполни ШАГ МОНИТОРИНГА off-site-ветки (после того как тексты реально размещены на площадках). Аргументы: $ARGUMENTS

Предусловия:
1. Есть `data/citation-gaps.md` (какие кластеры были off-site gap) и `data/placements.md` (кого отслеживать)?
2. Оператор положил СВЕЖИЕ выгрузки SERP по gap-кластерам в `input/serp/` (как в /tripl-collect — брать AI-обзор).
   Нет свежих данных — ОСТАНОВИСЬ, запроси: мониторинг идёт по новым снимкам, не по старым.

Запусти агента **geo-monitor**. Он перезапускает логику brand-presence serp-parser по gap-кластерам на свежих
выгрузках: цитируется ли целевой домен бренда в тексте AI-обзора ТЕПЕРЬ; кого отслеживать по citation share — из
`data/placements.md`. Выход: `review/geo-report.md` — какие gap'ы закрылись (бренд теперь цитируется), какие ещё
открыты, приоритетные следующие цели.

⛔ Покажи оператору: закрытые vs открытые gap'ы, дельта citation share, рекомендации (добить площадку / новый клайм).
Мониторинг периодический — запускать после каждой волны размещений с задержкой на индексацию.
